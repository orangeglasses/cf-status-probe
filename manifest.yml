applications:
- name: cf-status-probe
  buildpack: go_buildpack
  instances: 2
  memory: 1024M
  disk_quota: 1024M
  routes:
  - route: cf-status-probe.cfapps.io
  services:
  - proberedis
  stack: cflinuxfs2
